<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.jinsung.adoda.gpmon.NetworkUnavailableActivity">

    <TextView
        android:id="@+id/textview_sademoticon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="@android:color/holo_red_light"
        android:text=":-("
        android:textSize="40dp"
        android:layout_marginTop="40dp"
        />

    <TextView android:id="@+id/textview_description"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:text="Server not accessible. \n Are you connected to the internet?"
        android:textAlignment="center"
        android:textColor="@android:color/holo_blue_dark"
        android:textSize="20dp"
        android:layout_below="@id/textview_sademoticon"
        />

    <Button  android:id="@+id/button_reload"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:text="Tap here to reload."
        android:textAlignment="center"
        android:textSize="20dp"
        android:background="@android:color/darker_gray"
        android:textColor="@android:color/secondary_text_light"
        android:layout_alignParentBottom="true"
        />

</RelativeLayout>
